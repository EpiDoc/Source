* namespace cleanup to handle the xml:lang attributes that etree is screwing up
* get rid of .bak in directory listings (how exclude?)
* what to do about xml in src like <xi:include href="cocoon:/conformance-aiegl-list-raw.xml"/> (see e.g. conformance-aiegl-list.xml)
* a script to assemble stuff for release, like putting the css in the right place etc.